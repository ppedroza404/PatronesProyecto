package com.proyecto.AtaquesEspeciales;

import decorador.decorador.AtaqueEspecial;

public class DobleMovimiento extends AtaqueEspecial {
    public DobleMovimiento(String nombre, String funcion) {
        super(nombre, funcion);
    }
}
